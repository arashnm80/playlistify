## guide
- set variables in `config.py` and `.env` (api keys, proxy, ...)
- set telegram channel username in `playlistify.py` without `@`
- run the `playlistify.py`

### list of created channels:
https://t.me/Playlistify_Channel